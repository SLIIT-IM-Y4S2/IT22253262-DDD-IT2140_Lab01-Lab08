# Screenshots

Evidence from the Lab 04 application running against SQL Server, each showing
that operation's confirmation dialog. The sheet requires all three.

| File | Operation |
|---|---|
| `Lab04_Insert.png` | INSERT |
| `Lab04_Update.png` | UPDATE |
| `Lab04_Delete.png` | DELETE |

The database was checked after each step: the insert added the row, the update
changed it, the delete left the table empty.
